apiVersion: v1
kind: Service
metadata:
  name: mdo-redis-combined
  labels:
    app: mdo-redis
    deployment-strategy: canary
spec:
  type: ClusterIP
  selector:
    app: mdo-redis
    deployment-strategy: canary
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
