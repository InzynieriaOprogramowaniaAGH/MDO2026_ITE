apiVersion: v1
kind: Service
metadata:
  name: mdo-redis-recreate
  labels:
    app: mdo-redis
    deployment-strategy: recreate
spec:
  type: ClusterIP
  selector:
    app: mdo-redis
    deployment-strategy: recreate
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
