apiVersion: v1
kind: Service
metadata:
  name: mdo-redis
  labels:
    app: mdo-redis
spec:
  type: ClusterIP
  selector:
    app: mdo-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
