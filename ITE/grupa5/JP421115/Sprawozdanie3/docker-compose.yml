version: '3.8'

services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    image: cjson-builder

  tester:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: cjson-tester
    depends_on:
      - builder
