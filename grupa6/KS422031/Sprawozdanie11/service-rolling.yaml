apiVersion: v1
kind: Service
metadata:
  name: kacper-nginx-rolling-service
spec:
  selector:
    app: kacper-nginx-rolling
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
