version: '3.8'

services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    image: kalkulator-build:latest

  tester:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: kalkulator-test:latest
    depends_on:
      - builder