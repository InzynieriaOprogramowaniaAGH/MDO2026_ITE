services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    image: hiredis-build

  tester:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: hiredis-test
    depends_on:
      - builder