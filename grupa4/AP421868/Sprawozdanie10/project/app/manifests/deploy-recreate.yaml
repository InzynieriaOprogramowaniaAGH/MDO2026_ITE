apiVersion: apps/v1
kind: Deployment
metadata:
  name: moja-aplikacja-recreate
spec:
  replicas: 4
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: moja-aplikacja
  template:
    metadata:
      labels:
        app: moja-aplikacja
    spec:
      containers:
      - name: app
        image: al5ksandra/moja-aplikacja:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 80