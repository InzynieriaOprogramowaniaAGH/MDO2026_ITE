version: '3.8'

services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    image: moja-aplikacja:build

  tester:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: moja-aplikacja:test
    depends_on:
      - builder