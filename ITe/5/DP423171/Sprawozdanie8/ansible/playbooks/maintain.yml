- name: Maintenance of all systems
  hosts: all
  become: true
  become_method: su
  tasks:
   - name: Ping hosts
     ansible.builtin.ping:
   - name: Orchestrate updates
     community.general.pacman:
       update_cache: true
       upgrade: true
