pipeline {
    agent any
    environment {
        APP_NAME = "mdo-app"
        VERSION = "1.0.${BUILD_NUMBER}"
    }
    stages {
        stage('Cleanup') {
            steps {
                sh 'docker system prune -f'
                deleteDir()
            }
        }
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build & Test') {
            steps {
                dir('grupa2/MF420115/Sprawozdanie02/05-Class') {
                    sh 'docker build --no-cache -t ${APP_NAME}:latest .'
                    sh 'echo "Uruchamiam testy jednostkowe..." && ls myapp'
                }
            }
        }
        stage('Deploy (Sandbox)') {
            steps {
                sh 'docker run --rm ${APP_NAME}:latest'
            }
        }
        stage('Publish') {
            steps {
                sh 'docker save ${APP_NAME}:latest | gzip > ${APP_NAME}-${VERSION}.tar.gz'
                archiveArtifacts artifacts: '*.tar.gz', fingerprint: true
            }
        }
    }
}
